// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: article.sql

package biomedb

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createArticle = `-- name: CreateArticle :one
INSERT INTO articles (
        authors,
        year,
        title,
        journal,
        verbatim,
        doi,
        comments,
        code
    )
VALUES (
        $1,
        $2,
        $3,
        $4,
        $5,
        $6,
        $7,
        $8
    )
RETURNING id, authors, year, title, journal, verbatim, doi, comments, code
`

type CreateArticleParams struct {
	Authors  []string    `json:"authors"`
	Year     int32       `json:"year"`
	Title    pgtype.Text `json:"title"`
	Journal  pgtype.Text `json:"journal"`
	Verbatim pgtype.Text `json:"verbatim"`
	Doi      pgtype.Text `json:"doi"`
	Comments pgtype.Text `json:"comments"`
	Code     string      `json:"code"`
}

func (q *Queries) CreateArticle(ctx context.Context, arg CreateArticleParams) (Article, error) {
	row := q.db.QueryRow(ctx, createArticle,
		arg.Authors,
		arg.Year,
		arg.Title,
		arg.Journal,
		arg.Verbatim,
		arg.Doi,
		arg.Comments,
		arg.Code,
	)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}

const deleteArticleByCode = `-- name: DeleteArticleByCode :one
DELETE FROM articles
WHERE code = $1
RETURNING id, authors, year, title, journal, verbatim, doi, comments, code
`

func (q *Queries) DeleteArticleByCode(ctx context.Context, code string) (Article, error) {
	row := q.db.QueryRow(ctx, deleteArticleByCode, code)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}

const deleteArticleByID = `-- name: DeleteArticleByID :one
DELETE FROM articles
WHERE id = $1
RETURNING id, authors, year, title, journal, verbatim, doi, comments, code
`

func (q *Queries) DeleteArticleByID(ctx context.Context, id pgtype.UUID) (Article, error) {
	row := q.db.QueryRow(ctx, deleteArticleByID, id)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}

const getArticleByCode = `-- name: GetArticleByCode :one
SELECT id, authors, year, title, journal, verbatim, doi, comments, code
FROM articles
WHERE code = $1
LIMIT 1
`

func (q *Queries) GetArticleByCode(ctx context.Context, code string) (Article, error) {
	row := q.db.QueryRow(ctx, getArticleByCode, code)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}

const getArticleByID = `-- name: GetArticleByID :one
SELECT id, authors, year, title, journal, verbatim, doi, comments, code
FROM articles
WHERE id = $1
LIMIT 1
`

func (q *Queries) GetArticleByID(ctx context.Context, id pgtype.UUID) (Article, error) {
	row := q.db.QueryRow(ctx, getArticleByID, id)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}

const listArticles = `-- name: ListArticles :many
SELECT id, authors, year, title, journal, verbatim, doi, comments, code
FROM articles
ORDER BY authors [1] ASC,
    year DESC,
    code ASC
`

func (q *Queries) ListArticles(ctx context.Context) ([]Article, error) {
	rows, err := q.db.Query(ctx, listArticles)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Article
	for rows.Next() {
		var i Article
		if err := rows.Scan(
			&i.ID,
			&i.Authors,
			&i.Year,
			&i.Title,
			&i.Journal,
			&i.Verbatim,
			&i.Doi,
			&i.Comments,
			&i.Code,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateArticleByCode = `-- name: UpdateArticleByCode :one
UPDATE articles
SET authors = $1,
    year = $2,
    title = $3,
    journal = $4,
    verbatim = $5,
    doi = $6,
    comments = $7,
    code = $8
WHERE code = $9
RETURNING id, authors, year, title, journal, verbatim, doi, comments, code
`

type UpdateArticleByCodeParams struct {
	Authors  []string    `json:"authors"`
	Year     int32       `json:"year"`
	Title    pgtype.Text `json:"title"`
	Journal  pgtype.Text `json:"journal"`
	Verbatim pgtype.Text `json:"verbatim"`
	Doi      pgtype.Text `json:"doi"`
	Comments pgtype.Text `json:"comments"`
	Code     string      `json:"code"`
	OldCode  string      `json:"old_code"`
}

func (q *Queries) UpdateArticleByCode(ctx context.Context, arg UpdateArticleByCodeParams) (Article, error) {
	row := q.db.QueryRow(ctx, updateArticleByCode,
		arg.Authors,
		arg.Year,
		arg.Title,
		arg.Journal,
		arg.Verbatim,
		arg.Doi,
		arg.Comments,
		arg.Code,
		arg.OldCode,
	)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}

const updateArticleByID = `-- name: UpdateArticleByID :one
UPDATE articles
SET authors = $1,
    year = $2,
    title = $3,
    journal = $4,
    verbatim = $5,
    doi = $6,
    comments = $7,
    code = $8
WHERE id = $9
RETURNING id, authors, year, title, journal, verbatim, doi, comments, code
`

type UpdateArticleByIDParams struct {
	Authors  []string    `json:"authors"`
	Year     int32       `json:"year"`
	Title    pgtype.Text `json:"title"`
	Journal  pgtype.Text `json:"journal"`
	Verbatim pgtype.Text `json:"verbatim"`
	Doi      pgtype.Text `json:"doi"`
	Comments pgtype.Text `json:"comments"`
	Code     string      `json:"code"`
	ID       pgtype.UUID `json:"id"`
}

func (q *Queries) UpdateArticleByID(ctx context.Context, arg UpdateArticleByIDParams) (Article, error) {
	row := q.db.QueryRow(ctx, updateArticleByID,
		arg.Authors,
		arg.Year,
		arg.Title,
		arg.Journal,
		arg.Verbatim,
		arg.Doi,
		arg.Comments,
		arg.Code,
		arg.ID,
	)
	var i Article
	err := row.Scan(
		&i.ID,
		&i.Authors,
		&i.Year,
		&i.Title,
		&i.Journal,
		&i.Verbatim,
		&i.Doi,
		&i.Comments,
		&i.Code,
	)
	return i, err
}
